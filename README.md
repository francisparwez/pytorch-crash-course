# PyTorch Crash Course

---

FIRSTLY DOWNLOAD uv BY EXECUTING THE FOLLOWING COMMANDS IN powershell (admin) =>
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
OR
pip install uv/pip3 install uv

---

EXECUTE THE FOLLOWING COMMANDS IN COMMAND LINE ONE AFTER THE OTHER:
cd "C:\Users\franc\OneDrive\Desktop\DATA SCIENCE\PyTorch Crash Course"
uv init
uv add torch jupyterlab numpy scikit-learn

---

ONCE INSTALLED RUN THE FOLLOWING CODE TO RUN JUPYTER LAB=>
uv run jupyter lab

---